<a href="{{ $url }}" class="content-card">
    <div class="rounded-lg overflow-hidden relative">
        <img class="" src="{{ $thumbnail }}">
        <div class="content-card-play absolute inset-0 flex items-center justify-center invisible opacity-0 z-10">
            <i class="show-play-icon icon-live text-4xl text-white"></i>
        </div>
    </div>
</a>
