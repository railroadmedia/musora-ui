export default class User {
    constructor(
        public id: string,
        public avatar: string,
        public name: string,
        public level: number,
        public border: string
    ) {
    }
}
