export default class User {
    constructor(
        public id: string,
        public avatar: string,
        public name: string,
        public membership: string,
        public level: string
    ) {
    }
}
